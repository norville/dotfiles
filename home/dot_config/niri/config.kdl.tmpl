// ══════════════════════════════════════════════════════════════════════════
//  niri — scrollable-tiling Wayland compositor
//
//  Managed by chezmoi: edit the templates in this directory, not the deployed
//  ~/.config/niri/*.kdl files. The config is split into per-section includes
//  (siblings of this file), which replace the cachyos-niri-noctalia package's
//  ./cfg/*.kdl set — that directory is no longer included, so the package can
//  no longer override this config.
//
//  Noctalia-shell-specific directives are gated on `lookPath "noctalia"` inside
//  the relevant include (appearance / rules / autostart / keybinds), so the
//  config still renders and validates on a machine without it.
// ══════════════════════════════════════════════════════════════════════════

include "./input.kdl"
include "./outputs.kdl"
include "./layout.kdl"
include "./misc.kdl"
include "./animations.kdl"
include "./rules.kdl"
include "./autostart.kdl"
include "./keybinds.kdl"
