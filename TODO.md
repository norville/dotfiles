# Dotfiles TODO

- [x] try alternatives to tab completion
- [x] rename log files: "<yyyymmdd>_<hhmmss>_<script name>_<id>.log"
- [x] 03-1password-setup.sh should check for pub keys and use log files too
- [x] del/end keys not working in zsh
- [x] sudo alias resolution
- [x] sudo zsh completions
- [x] sudo neovim editing
- [x] fresh-editor error on ubuntu, install via `sudo snap install fresh-editor --classic`
- [x] fedora: install `fresh` via terra - [https://github.com/terrapkg/packages]
- [x] debian: chezmoi fails because of `/snap/bin` missing from path
- [x] cachyos: bootstrap fails when installing dependencies
- [x] cachyos: yay asks sudo passwd
- [x] optional packages *1password*, *vscode*: ask for installation only when `$machine = workstation`
- [x] 1password setup: `workstation` -> ask for setup; `terminal` -> do not setup
- [x] swap `command -v` with `bdb_test_cmd`
- [ ] zoxide: `zi` non working
- [x] ensure 1password ui is running before executing setup
- [ ] starship: powerline slanted with moon colors
- [ ] automate macos configuration via 'defaults'
- [ ] test usage of ansible over bdb
