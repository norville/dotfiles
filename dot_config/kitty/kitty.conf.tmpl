## Color scheme
include             Tokyo-Night-Moon.conf
background_opacity  0.95

## Fonts
{{ if eq .chezmoi.os "linux" -}}
# Linux uses the plain "Nerd Font" variant at 12pt (HiDPI handled by compositor).
font_family       family='JetBrainsMono Nerd Font' style=Regular
font_size         12.0
{{ else if eq .chezmoi.os "darwin" -}}
# macOS uses the "Nerd Font Mono" variant at 15pt (Retina display scaling).
font_family       family='JetBrainsMono Nerd Font Mono' style=Regular
font_size         15.0
{{ end -}}
bold_font         auto
bold_italic_font  auto
disable_ligatures always
font_features     none
italic_font       auto

## Text cursor customization
cursor_shape            block
cursor_shape_unfocused  hollow

## Scrollback
scrollback_lines              5000
scrollback_pager_history_size 10
scrollbar                     never

## Mouse
strip_trailing_spaces smart

## Mouse actions
#

## Performance tuning
#

## Terminal bell
#

## Window layout
enabled_layouts           tall:bias=50;full_size=1;mirrored=false
remember_window_size      yes
remember_window_position  yes
window_padding_width      4
window_border_width       2
hide_window_decorations   titlebar-only

## Tab bar
tab_bar_min_tabs  1
tab_bar_edge      bottom
tab_bar_style     custom

## Advanced
shell_integration no-cursor

## Remote control
# socket-only: kitty @ commands work from child processes (e.g. load-session.sh)
# but the socket is not exposed over TCP. listen_on creates a per-instance socket.
allow_remote_control  socket-only
listen_on             unix:/tmp/kitty-{kitty_pid}

## OS specific tweaks
#

## Keyboard shortcuts
#

## Clipboard
#

## Scrolling
#

## Window management
#

## Tab management
#

## Layout management
# switch to next layout
# next/prev window

## Font sizes
#

## Miscellaneous
# close ssh connections
map ctrl+alt+super+k  close_shared_ssh_connections

## Sessions
# save window state to a session file; prompts for name, default dir: ~/.config/kitty/sessions/
map f1 save_as_session --use-foreground-process --base-dir ~/.config/kitty/sessions

# fzf picker — opens the selected session in a new tab (not a new OS window)
map f4 launch --type=overlay bash ~/.config/kitty/load-session.sh
