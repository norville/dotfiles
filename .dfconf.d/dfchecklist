### Dotfiles checklist ###

## Actions to perform before running dfboot
# commit and push any changes/branches to your git repositories
# update mackup to the latest version and run mackup backup

## Setup new macOS system
# macOS clean install
# macOS update
# Mac App Store login
# install Xcode CLT: xcode-select --install
# import SSH keys
