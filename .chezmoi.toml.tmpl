{{- /* Detect OS and Linux distro */ -}}
{{- $osid := .chezmoi.os -}}
{{- if hasKey .chezmoi.osRelease "id" -}}
    {{- $osid = printf "%s-%s" .chezmoi.os .chezmoi.osRelease.id -}}
{{- end -}}

{{- /* Get machine vendor from env variable set by bootstrap script */ -}}
{{- $vendor := "Unknown" -}}
{{- if (or (eq .chezmoi.os "linux") (eq .chezmoi.os "darwin")) -}}
{{-     $vendor = (env "PC_VENDOR" | trim | default "Unknown") -}}
{{- else if eq .osid "windows" -}}
{{- end -}}

# Define chezmoi variables to be used in templates
[data]
    vendor = {{ $vendor | quote }}
    osid = {{ $osid | quote }}
    email = "ing.norville@protonmail.com"

# Define ENV variables for shell scripts
[scriptEnv]
    # Path to helper functions
    HELPERS = ".config/bdb/bdb_helpers.sh"
