README.md
.config/bdb/bdb_bootstrap.sh

# ignore these unless OS is darwin
{{- if ne .chezmoi.os "darwin" }}
.config/homebrew/
{{- end }}

# ignore these unless OS is Manjaro runinng on Mac
{{- if (or (ne .osid "linux-manjaro") (ne .vendor "Apple")) -}}
/etc/modprobe.d/alsa-base.conf
{{- end }}

# ignore these unless OS is either Manjaro or macOS
{{- if (and (ne .osid "linux-manjaro") (ne .chezmoi.os "darwin")) -}}
.ssh/config
{{- end }}
