README.md

# ignore these files because content is managed externally
.config/iterm2/preferences.plist
.config/moom/preferences.plist

# ignore these files unless OS is darwin
{{-     if ne .chezmoi.os "darwin" }}
.config/homebrew/
.config/iterm2/
Library/
{{-     end }}

# ignore these files unless OS is linux
{{-     if ne .chezmoi.os "linux" }}
.config/alacritty/
{{-     end }}

# ignore these files unless OS is windows
{{-    if ne .chezmoi.os "windows" }}
{{-    end }}

