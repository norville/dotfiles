README.md

# ignore these files on maOS because content is managed externally
{{-     if eq .chezmoi.os "darwin" }}
.config/moom/com.manytricks.Moom.plist
.config/iterm2/com.googlecode.iterm2.plist
{{-     end }}

# ignore these files unless OS is darwin
{{-     if ne .chezmoi.os "darwin" }}
.config/homebrew/
{{-     end }}

# ignore these files unless OS is linux
{{-     if ne .chezmoi.os "linux" }}
.config/alacritty/
{{-     end }}

# ignore these files unless OS is windows
{{-    if ne .chezmoi.os "windows" }}
{{-    end }}

